<% if (defined?(sidebar_tags)) && User.current.allowed_to?(:issue_view_tags, @project) && !sidebar_tags.empty? && !@issue %>
  <%= stylesheet_link_tag 'jquery.tagit.css', plugin: 'redmine_tags' %>
  <%= stylesheet_link_tag 'redmine_tags', plugin: 'redmine_tags' %>
  <h3><%= l :tags %></h3>
  <%= render_sidebar_tags %>
<% end %>
